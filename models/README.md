# Models

Trained model files are not committed to the repository.

Large model files should be stored externally, for example:

- Google Drive
- GitHub Releases
- Kaggle Models
- Hugging Face Hub

Expected local structure:

```text
models/
├── mobilenetv2_best.pth
├── efficientnetb2_best.pth
├── resnet50_best.pth
└── resnet50_finetuned_best.pth
```

Add model download links here if needed:

```text
Best model: TODO
```
